// Configuration settings for the application
const config = {
  // API URL from environment variable or fallback to the default
  apiUrl: process.env.REACT_APP_API_URL || "https://fuchr--orpheus-tts-api-refined-v2-orpheusttsapi-run-fastapi-app.modal.run"
};

export default config; 