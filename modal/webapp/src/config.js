// Configuration settings for the application
const config = {
  // API URL from environment variable or fallback to the new 10-speaker API
  apiUrl: process.env.REACT_APP_API_URL || "https://fuchr--orpheus-tts-api-10speakers-v2-orpheusttsapi-run-f-783b4c.modal.run"
};

export default config; 